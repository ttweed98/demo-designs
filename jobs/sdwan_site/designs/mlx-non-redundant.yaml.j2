{% import 'designs/create_macro.j2' as create %}

{% set device_type = 'cisco8200' %}
{% set device_role = 'wanswitch' %}
{% set status_name = 'Active' %}
{% set protocol = 'vrrp' %}
{% set device_manufacturer = 'cisco' %}

device_types:
  {{ create.device_type(device_type, device_manufacturer) }}

prefixes:
    {{ create.prefix(site_prefix, site_name, status_name, 'Site Subnet', tenant_name) }}
    {{ create.prefix(management_prefix, site_name, status_name, 'KGS Management Prefixes', tenant_name) }}